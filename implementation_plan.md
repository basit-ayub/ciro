# Goal: Complete CIRO Master Plan Backlog & Finalize Hackathon Project

This plan details the steps required to complete the remaining tasks outlined in the `CIRO_Implementation_Log.md` for the Phase 1-5 deliverables. It covers environment setup (Flutter), backend enhancements (Voice intake, Demo mode), UI completion, mock data generation, and documentation updates.

## User Review Required

> [!IMPORTANT]
> **Manual Actions Needed**
> Because I am an AI running in this workspace, there are certain tasks I cannot complete autonomously. These must be handled by you (the USER):
> - **Video/Media Creation**: Tasks 4.3 (Splice Antigravity Manager view) and 4.9 (Demo video filming).
> - **Screenshots**: Tasks 2.14 and 4.7 (Capturing IDE/Manager view screenshots).
> - **Cloud Deployment/App Release**: Phase 5 tasks (5.1-5.7) such as Cloud Run deployment, Firebase App Distribution, and setting up Twilio numbers. I can provide the configuration and commands, but the actual execution and testing on cloud infrastructure is manual.

> [!CAUTION]
> **Flutter Installation**
> I do not have `sudo` privileges. I will download the Flutter SDK binary archive to `/home/abdulbasit/flutter` and set it up locally in the workspace to fulfill the Flutter UI tasks.

## Open Questions

> [!NOTE]
> 1. Do you have a preferred Firebase project already set up, or should the Flutter app use mock/local dummy services for UI compilation purposes during this phase?
> 2. For the Roman Urdu language polish (Task 4.10), do you want to restrict the output strictly to code-mixed Urdu/English, or allow standard English fallback?

## Proposed Changes

### 1. Environment & Setup

- **Action**: Download and extract the Flutter SDK for Linux to `/home/abdulbasit/flutter`. Update path dynamically for execution.
- **Action**: Run `flutter create .` inside `mobile/` (or initialize if not yet a full project structure) and run `flutter pub get`.

---

### 2. Mock Data Generation

#### [MODIFY] `mock-data/scenarios.json`
- Expand from the current state to include 5 distinct, fully scripted scenarios (Task 2.13) with timestamps, geolocation boundaries, and event triggers for Hell Mode testing.

---

### 3. Backend & Core Services

#### [NEW] `backend/routers/voice.py`
- Implement Task 4.1: A webhook for Twilio voice intake, simulating Speech-to-Text conversion and forwarding to the Sentinel agent.

#### [MODIFY] `backend/services/demo_mode.py`
- Implement Task 4.2: Add logic for artificial latency (to simulate processing), prompt-hash caching, and triggering predefined scenarios to ensure stable demos.

#### [MODIFY] `backend/prompts/commander.txt`
- Implement Task 4.10: Polish the prompt instructions to guarantee high-quality Roman Urdu / Code-mixed alert responses.

---

### 4. Flutter Mobile UI Implementation

*Since `mobile/` currently has scaffolded files, I will update them to full functional UI.*

#### [MODIFY] `mobile/lib/screens/map_screen.dart` (or similar)
- Implement Tasks 2.12 and 3.7: Add animated rerouting polylines for crisis detours and a 5-colored zone overlay for Hell Mode.

#### [MODIFY] `mobile/lib/widgets/live_reasoning_stadium.dart` (or similar)
- Implement Task 1.8: Add Firebase firestore mock stream listeners to visualize `triage_queue` items.

#### [MODIFY] `mobile/lib/widgets/twin_timeline.dart`
- Implement Tasks 4.4 and 4.5: Add robust loading/error states and a replay/scrubber control for the incident timeline.

---

### 5. Documentation & Logs

#### [MODIFY] `CIRO_Implementation_Log.md`
- Update task statuses from ⬜ to ✅ as we progress through Phase 1-5 tasks.

#### [MODIFY] `logs/build_log.md`
- Append detailed trace logs (Session 012 onwards) containing the reasoning, actions, and code snippets generated by me during this session to satisfy the hackathon log requirement.

#### [MODIFY] `README.md`
- Complete Task 4.8: Write the final comprehensive README with all 12 sections required for submission.

## Verification Plan

### Automated Tests
1. Run `python -m pytest` (if tests exist) or boot the FastAPI server using `fastapi run backend/main.py` to ensure no syntax/runtime errors in the new backend files.
2. Run `flutter analyze` inside `mobile/` to verify the UI code is valid.

### Manual Verification
- Review the `logs/build_log.md` to confirm detailed AI tracing is present.
- Open the updated `README.md` to ensure the project looks ready for submission.
